using System;
using System.Collections.Generic;
using Newtonsoft.Json;
using System.Data;
using System.Diagnostics;

namespace PizzaProject
{
    //Not sure if this class is necessary or not, but we'll find out soon :)
    public class JSONHandler
    {
        //Comment writing class outline idea
        
        //ALL WRONG but do not change
        //Working on understanding how to write the handler
        
        //private string orderDB = "whatever the file path is";
        //private string customerDB = "whatever the file path is";
        //private string userDB = "whatever the file path is";
        
        //public void orderJSON(List<Order> orders, string type) {
        // if (type == "read" || type == "Read") {
        //    try/catch JsonConvert.SerializeObject(orders);
        //   }
        // if (type == "write" || type == "Write") {
        //    try/catch JsonConvert.DeserializeObject(Order);
        //    return list of Orders;
        //   }
        // }
        
        //public void customerJSON(List<Customer> customers, string type) {
        // if (type == "read" || type == "Read") {
        //    try/catch JsonConvert.SerializeObject(cusotmers);
        //   }
        // if (type == "write" || type == "Write") {
        //    try/catch JsonConvert.DeserializeObject(Customer);
        //    return list of Customers;
        //   }
        // }
        
        //public void userJSON(List<User> users, string type) {
        // if (type == "read" || type == "Read") {
        //    try/catch JsonConvert.SerializeObject(users);
        //   }
        // if (type == "write" || type == "Write") {
        //    try/catch JsonConvert.DeserializeObject(User);
        //    return list of Users;
        //   }
        // }
        
        //public void CheckJSONFile() {
        // try { if (Application.datapath(orderDB) == null/false) {
        //          Add Orders.json; }
        //     }
        // catch {}
        // try { if (Application.datapath(customerDB) == null/false) {
        //          Add Customers.json; }
        //     }
        // catch {}
        // try { if (Application.datapath(userDB) == null/false) {
        //          Add Users.json; }
        //     }
        // catch {}
        // }
    }
}
